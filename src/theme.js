import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#fff",
      light: "#25252515",
    },
    secondary: {
      main: "#fff",
    },
    filler: {
      main: "#eb4848",
    },
  },
});
